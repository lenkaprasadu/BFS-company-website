<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0"/>
  <title>prasaduworld.com</title>
  <link href="https://fonts.googleapis.com/css2?family=Poppins:wght@300;400;600;700;800&family=Playfair+Display:wght@700&display=swap" rel="stylesheet"/>
  <style>
    *, *::before, *::after { margin: 0; padding: 0; box-sizing: border-box; }

    :root {
      --blue: #1565C0;
      --blue-dark: #0D47A1;
      --blue-light: #1E88E5;
      --accent: #42A5F5;
      --white: #ffffff;
      --off-white: #F0F6FF;
      --text: #1a2a3a;
      --text-light: #5a6a7a;
    }

    html { scroll-behavior: smooth; }

    body {
      font-family: 'Poppins', sans-serif;
      background: var(--white);
      color: var(--text);
      overflow-x: hidden;
    }

    /* ── NAVBAR ── */
    nav {
      position: fixed; top: 0; width: 100%; z-index: 999;
      background: rgba(255,255,255,0.95);
      backdrop-filter: blur(10px);
      border-bottom: 2px solid var(--blue);
      display: flex; align-items: center; justify-content: space-between;
      padding: 0 6%; height: 68px;
      box-shadow: 0 2px 20px rgba(21,101,192,0.12);
      transition: box-shadow 0.3s;
    }
    .logo {
      font-family: 'Playfair Display', serif;
      font-size: 1.9rem;
      color: var(--blue);
      letter-spacing: 2px;
      user-select: none;
    }
    .logo span { color: var(--accent); }
    .nav-links { display: flex; gap: 32px; list-style: none; }
    .nav-links a {
      text-decoration: none; font-size: 0.88rem; font-weight: 600;
      color: var(--text); letter-spacing: 0.5px;
      position: relative; padding-bottom: 4px;
      transition: color 0.3s;
    }
    .nav-links a::after {
      content: ''; position: absolute; bottom: 0; left: 0;
      width: 0; height: 2px; background: var(--blue);
      transition: width 0.3s;
    }
    .nav-links a:hover { color: var(--blue); }
    .nav-links a:hover::after { width: 100%; }
    .nav-cta {
      background: var(--blue); color: #fff;
      border: none; padding: 10px 24px; border-radius: 30px;
      font-family: 'Poppins', sans-serif; font-size: 0.88rem; font-weight: 600;
      cursor: pointer; transition: background 0.3s, transform 0.2s;
    }
    .nav-cta:hover { background: var(--blue-dark); transform: translateY(-1px); }
    .hamburger { display: none; flex-direction: column; gap: 5px; cursor: pointer; }
    .hamburger span { width: 26px; height: 3px; background: var(--blue); border-radius: 2px; transition: 0.3s; }

    /* ── HERO ── */
    #hero {
      min-height: 100vh;
      background: var(--white);
      display: flex; align-items: center;
      padding: 100px 6% 60px;
      position: relative; overflow: hidden;
    }
    .hero-bg-shape {
      position: absolute; right: -80px; top: 50%;
      transform: translateY(-50%);
      width: 520px; height: 520px;
      background: radial-gradient(circle, rgba(21,101,192,0.10) 0%, rgba(66,165,245,0.05) 70%, transparent 100%);
      border-radius: 50%;
      animation: pulse 5s ease-in-out infinite;
    }
    @keyframes pulse { 0%,100%{transform:translateY(-50%) scale(1);} 50%{transform:translateY(-50%) scale(1.07);} }
    .hero-content { max-width: 620px; position: relative; z-index: 1; }
    .hero-badge {
      display: inline-block;
      background: var(--off-white); color: var(--blue);
      font-size: 0.78rem; font-weight: 700; letter-spacing: 1.5px;
      padding: 6px 18px; border-radius: 20px; border: 1.5px solid var(--accent);
      margin-bottom: 24px; text-transform: uppercase;
      animation: fadeDown 0.8s ease both;
    }
    .hero-title {
      font-family: 'Playfair Display', serif;
      font-size: clamp(2.4rem, 5vw, 3.6rem);
      color: var(--blue);
      line-height: 1.18;
      animation: fadeUp 0.9s ease 0.1s both;
    }
    .hero-title span { color: var(--accent); }
    .hero-sub {
      margin-top: 18px; font-size: 1.05rem; color: var(--text-light);
      line-height: 1.8; animation: fadeUp 0.9s ease 0.2s both;
    }
    .hero-btns {
      margin-top: 36px; display: flex; gap: 16px; flex-wrap: wrap;
      animation: fadeUp 0.9s ease 0.3s both;
    }
    .btn-primary {
      background: var(--blue); color: #fff;
      padding: 14px 32px; border-radius: 32px; border: none;
      font-family: 'Poppins', sans-serif; font-size: 0.95rem; font-weight: 600;
      cursor: pointer; transition: background 0.3s, transform 0.2s, box-shadow 0.3s;
      box-shadow: 0 6px 20px rgba(21,101,192,0.3);
    }
    .btn-primary:hover { background: var(--blue-dark); transform: translateY(-2px); box-shadow: 0 10px 28px rgba(21,101,192,0.4); }
    .btn-outline {
      background: transparent; color: var(--blue);
      padding: 14px 32px; border-radius: 32px;
      border: 2px solid var(--blue);
      font-family: 'Poppins', sans-serif; font-size: 0.95rem; font-weight: 600;
      cursor: pointer; transition: all 0.3s;
    }
    .btn-outline:hover { background: var(--blue); color: #fff; }
    .hero-stats {
      margin-top: 50px; display: flex; gap: 40px; flex-wrap: wrap;
      animation: fadeUp 0.9s ease 0.4s both;
    }
    .stat { text-align: left; }
    .stat-num { font-size: 2rem; font-weight: 800; color: var(--blue); line-height: 1; }
    .stat-label { font-size: 0.78rem; color: var(--text-light); font-weight: 500; margin-top: 4px; letter-spacing: 0.5px; }

    /* ── SECTION COMMON ── */
    section { padding: 90px 6%; }
    .section-tag {
      display: inline-block; background: var(--off-white);
      color: var(--blue); font-size: 0.75rem; font-weight: 700;
      letter-spacing: 2px; text-transform: uppercase;
      padding: 5px 16px; border-radius: 20px; margin-bottom: 12px;
    }
    .section-title {
      font-family: 'Playfair Display', serif;
      font-size: clamp(1.8rem, 3.5vw, 2.5rem);
      color: var(--blue); line-height: 1.25; margin-bottom: 14px;
    }
    .section-desc { font-size: 1rem; color: var(--text-light); max-width: 560px; line-height: 1.8; }

    /* ── SERVICES ── */
    #services { background: var(--off-white); }
    .services-grid {
      display: grid;
      grid-template-columns: repeat(auto-fit, minmax(260px, 1fr));
      gap: 28px; margin-top: 50px;
    }
    .service-card {
      background: var(--white); border-radius: 18px;
      padding: 36px 28px;
      border: 1.5px solid rgba(21,101,192,0.1);
      transition: transform 0.3s, box-shadow 0.3s, border-color 0.3s;
      cursor: default;
      position: relative; overflow: hidden;
    }
    .service-card::before {
      content: ''; position: absolute; top: 0; left: 0;
      width: 100%; height: 4px;
      background: linear-gradient(90deg, var(--blue), var(--accent));
      transform: scaleX(0); transform-origin: left;
      transition: transform 0.4s;
    }
    .service-card:hover::before { transform: scaleX(1); }
    .service-card:hover { transform: translateY(-8px); box-shadow: 0 20px 50px rgba(21,101,192,0.13); border-color: var(--accent); }
    .service-icon {
      width: 60px; height: 60px; border-radius: 14px;
      background: var(--off-white);
      display: flex; align-items: center; justify-content: center;
      font-size: 1.8rem; margin-bottom: 22px;
      transition: background 0.3s;
    }
    .service-card:hover .service-icon { background: rgba(21,101,192,0.1); }
    .service-card h3 { font-size: 1.15rem; font-weight: 700; color: var(--blue); margin-bottom: 10px; }
    .service-card p { font-size: 0.9rem; color: var(--text-light); line-height: 1.75; }
    .service-tag {
      display: inline-block; margin-top: 18px;
      background: var(--off-white); color: var(--blue);
      font-size: 0.72rem; font-weight: 700; padding: 4px 12px; border-radius: 20px;
    }

    /* ── WHY US ── */
    #why { background: var(--white); }
    .why-grid {
      display: grid;
      grid-template-columns: repeat(auto-fit, minmax(220px, 1fr));
      gap: 24px; margin-top: 50px;
    }
    .why-item {
      display: flex; gap: 16px; align-items: flex-start;
      padding: 24px 20px; border-radius: 14px;
      border: 1.5px solid rgba(21,101,192,0.1);
      transition: box-shadow 0.3s, border-color 0.3s;
    }
    .why-item:hover { box-shadow: 0 8px 30px rgba(21,101,192,0.1); border-color: var(--accent); }
    .why-num {
      min-width: 44px; height: 44px; border-radius: 12px;
      background: var(--blue); color: #fff;
      font-weight: 800; font-size: 1rem;
      display: flex; align-items: center; justify-content: center;
    }
    .why-item h4 { font-size: 0.95rem; font-weight: 700; color: var(--blue); margin-bottom: 5px; }
    .why-item p { font-size: 0.85rem; color: var(--text-light); line-height: 1.6; }

    /* ── PRICING ── */
    #pricing { background: var(--off-white); }
    .pricing-grid {
      display: grid;
      grid-template-columns: repeat(auto-fit, minmax(260px, 1fr));
      gap: 28px; margin-top: 50px;
    }
    .price-card {
      background: var(--white); border-radius: 20px;
      padding: 40px 32px; border: 2px solid rgba(21,101,192,0.1);
      transition: transform 0.3s, box-shadow 0.3s;
      position: relative;
    }
    .price-card.popular {
      border-color: var(--blue);
      box-shadow: 0 12px 40px rgba(21,101,192,0.18);
      transform: scale(1.03);
    }
    .popular-badge {
      position: absolute; top: -14px; left: 50%; transform: translateX(-50%);
      background: var(--blue); color: #fff;
      font-size: 0.72rem; font-weight: 700; letter-spacing: 1px;
      padding: 5px 18px; border-radius: 20px; text-transform: uppercase;
    }
    .price-card:hover { transform: translateY(-6px); box-shadow: 0 20px 50px rgba(21,101,192,0.15); }
    .price-card.popular:hover { transform: scale(1.03) translateY(-6px); }
    .price-plan { font-size: 0.8rem; font-weight: 700; text-transform: uppercase; letter-spacing: 1.5px; color: var(--blue); margin-bottom: 10px; }
    .price-amount { font-size: 2.6rem; font-weight: 800; color: var(--text); line-height: 1; }
    .price-amount span { font-size: 1rem; font-weight: 500; color: var(--text-light); }
    .price-desc { font-size: 0.85rem; color: var(--text-light); margin: 10px 0 24px; line-height: 1.6; }
    .price-features { list-style: none; margin-bottom: 32px; }
    .price-features li {
      font-size: 0.88rem; color: var(--text); padding: 8px 0;
      border-bottom: 1px solid rgba(21,101,192,0.07);
      display: flex; gap: 10px; align-items: center;
    }
    .price-features li::before { content: '✓'; color: var(--blue); font-weight: 800; font-size: 1rem; }
    .price-btn {
      width: 100%; padding: 13px; border-radius: 30px; border: 2px solid var(--blue);
      font-family: 'Poppins', sans-serif; font-size: 0.9rem; font-weight: 700;
      cursor: pointer; transition: all 0.3s;
      background: transparent; color: var(--blue);
    }
    .price-card.popular .price-btn { background: var(--blue); color: #fff; }
    .price-btn:hover { background: var(--blue); color: #fff; }

    /* ── CONTACT ── */
    #contact { background: var(--white); }
    .contact-wrap {
      display: grid; grid-template-columns: 1fr 1fr; gap: 60px;
      margin-top: 50px; align-items: start;
    }
    .contact-info h3 { font-size: 1.2rem; font-weight: 700; color: var(--blue); margin-bottom: 16px; }
    .contact-info p { font-size: 0.95rem; color: var(--text-light); line-height: 1.8; margin-bottom: 28px; }
    .contact-detail { display: flex; gap: 14px; align-items: center; margin-bottom: 16px; }
    .contact-detail-icon {
      width: 44px; height: 44px; border-radius: 12px;
      background: var(--off-white); display: flex; align-items: center;
      justify-content: center; font-size: 1.2rem; flex-shrink: 0;
    }
    .contact-detail span { font-size: 0.88rem; color: var(--text-light); line-height: 1.5; }
    .contact-detail strong { display: block; color: var(--text); font-size: 0.92rem; }
    .contact-form { display: flex; flex-direction: column; gap: 16px; }
    .form-group { display: flex; flex-direction: column; gap: 6px; }
    .form-group label { font-size: 0.82rem; font-weight: 600; color: var(--blue); letter-spacing: 0.3px; }
    .form-group input, .form-group textarea, .form-group select {
      padding: 12px 16px; border-radius: 10px;
      border: 1.5px solid rgba(21,101,192,0.2);
      font-family: 'Poppins', sans-serif; font-size: 0.9rem;
      color: var(--text); background: var(--white);
      transition: border-color 0.3s, box-shadow 0.3s;
      outline: none;
    }
    .form-group input:focus, .form-group textarea:focus, .form-group select:focus {
      border-color: var(--blue); box-shadow: 0 0 0 3px rgba(21,101,192,0.1);
    }
    .form-group textarea { resize: vertical; min-height: 110px; }
    .form-row { display: grid; grid-template-columns: 1fr 1fr; gap: 16px; }
    .form-submit {
      background: var(--blue); color: #fff; border: none;
      padding: 14px 36px; border-radius: 30px;
      font-family: 'Poppins', sans-serif; font-size: 0.95rem; font-weight: 700;
      cursor: pointer; transition: background 0.3s, transform 0.2s;
      align-self: flex-start;
    }
    .form-submit:hover { background: var(--blue-dark); transform: translateY(-2px); }
    .form-success {
      display: none; background: #E3F2FD; border: 1.5px solid var(--blue);
      color: var(--blue); padding: 14px 20px; border-radius: 10px;
      font-size: 0.9rem; font-weight: 600; text-align: center;
    }

    /* ── FOOTER ── */
    footer {
      background: var(--blue-dark); color: rgba(255,255,255,0.8);
      text-align: center; padding: 36px 6%;
    }
    .footer-logo { font-family: 'Playfair Display', serif; font-size: 1.6rem; color: #fff; margin-bottom: 10px; }
    .footer-logo span { color: var(--accent); }
    footer p { font-size: 0.85rem; line-height: 1.8; }
    .footer-links { display: flex; justify-content: center; gap: 28px; margin: 14px 0; flex-wrap: wrap; }
    .footer-links a { color: rgba(255,255,255,0.7); text-decoration: none; font-size: 0.85rem; transition: color 0.3s; }
    .footer-links a:hover { color: var(--accent); }

    /* ── ANIMATIONS ── */
    @keyframes fadeUp { from { opacity:0; transform:translateY(28px); } to { opacity:1; transform:translateY(0); } }
    @keyframes fadeDown { from { opacity:0; transform:translateY(-16px); } to { opacity:1; transform:translateY(0); } }
    .reveal { opacity: 0; transform: translateY(30px); transition: opacity 0.7s ease, transform 0.7s ease; }
    .reveal.visible { opacity: 1; transform: translateY(0); }

    /* ── SCROLL TOP ── */
    #scrollTop {
      position: fixed; bottom: 30px; right: 30px; z-index: 999;
      width: 46px; height: 46px; border-radius: 50%;
      background: var(--blue); color: #fff; border: none;
      font-size: 1.2rem; cursor: pointer;
      box-shadow: 0 6px 20px rgba(21,101,192,0.35);
      display: none; align-items: center; justify-content: center;
      transition: background 0.3s, transform 0.2s;
    }
    #scrollTop:hover { background: var(--blue-dark); transform: translateY(-2px); }
    #scrollTop.show { display: flex; }

    /* ── MOBILE NAV ── */
    .mobile-menu {
      display: none; flex-direction: column; gap: 0;
      position: fixed; top: 68px; left: 0; width: 100%;
      background: #fff; border-bottom: 2px solid var(--blue);
      z-index: 998; padding: 10px 0;
    }
    .mobile-menu.open { display: flex; }
    .mobile-menu a {
      text-decoration: none; padding: 14px 6%;
      font-size: 0.95rem; font-weight: 600; color: var(--text);
      border-bottom: 1px solid rgba(21,101,192,0.08);
      transition: background 0.2s;
    }
    .mobile-menu a:hover { background: var(--off-white); color: var(--blue); }

    /* ── RESPONSIVE ── */
    @media (max-width: 768px) {
      .nav-links, .nav-cta { display: none; }
      .hamburger { display: flex; }
      .contact-wrap { grid-template-columns: 1fr; gap: 40px; }
      .form-row { grid-template-columns: 1fr; }
      .price-card.popular { transform: scale(1); }
    }
  </style>
</head>
<body>

<!-- NAVBAR -->
<nav id="navbar">
  <div class="logo">Prasad BFS<span>.</span></div>
  <ul class="nav-links">
    <li><a href="#hero">Home</a></li>
    <li><a href="#services">Services</a></li>
    <li><a href="#why">Why Us</a></li>
    <li><a href="#pricing">Pricing</a></li>
    <li><a href="#contact">Contact</a></li>
  </ul>
  <button class="nav-cta" onclick="document.getElementById('contact').scrollIntoView({behavior:'smooth'})">Hire Me</button>
  <div class="hamburger" id="hamburger" onclick="toggleMenu()">
    <span></span><span></span><span></span>
  </div>
</nav>

<!-- MOBILE MENU -->
<div class="mobile-menu" id="mobileMenu">
  <a href="#hero" onclick="closeMenu()">Home</a>
  <a href="#services" onclick="closeMenu()">Services</a>
  <a href="#why" onclick="closeMenu()">Why Us</a>
  <a href="#pricing" onclick="closeMenu()">Pricing</a>
  <a href="#contact" onclick="closeMenu()">Contact</a>
</div>

<!-- HERO -->
<section id="hero">
  <div class="hero-bg-shape"></div>
  <div class="hero-content">
    <div class="hero-badge">✦ Best Freelancing Services</div>
    <h1 class="hero-title">
      Creative Work.<br>
      <span>Low Cost.</span><br>
      High Quality.
    </h1>
    <p class="hero-sub">
      Welcome to <strong>BFS Company</strong> — your trusted partner for professional <strong>Website Design</strong> and <strong>Video Editing</strong>. We deliver stunning results at affordable prices, tailored to your needs.
    </p><br>
    <p>I am B.Tech student Specialised in Electrical and Electronics Engineering (EEE) at WISTM</p>
    <div class="hero-btns">
      <button class="btn-primary" onclick="document.getElementById('services').scrollIntoView({behavior:'smooth'})">Explore Services</button>
      <button class="btn-outline" onclick="document.getElementById('contact').scrollIntoView({behavior:'smooth'})">Get a Quote</button>
    </div>
    <div class="hero-stats">
      <div class="stat"><div class="stat-num" data-count="120">0</div><div class="stat-label">Projects Done</div></div>
      <div class="stat"><div class="stat-num" data-count="98">0</div><div class="stat-label">Happy Clients</div></div>
      <div class="stat"><div class="stat-num" data-count="2">0</div><div class="stat-label">Years Experience</div></div>
    </div>
  </div>
</section>

<!-- SERVICES -->
<section id="services">
  <div class="reveal">
    <span class="section-tag">What We Offer</span>
    <h2 class="section-title">Our Core Services</h2>
    <p class="section-desc">Professional services tailored for your business — delivered with creativity, speed, and care.</p>
  </div>
  <div class="services-grid reveal">
    <div class="service-card">
      <div class="service-icon">🌐</div>
      <h3>Website Design</h3>
      <p>Beautiful, responsive, and user-friendly websites built from scratch. Clean code, fast loading, and mobile optimised for every screen.</p>
      <span class="service-tag">HTML • CSS • JS</span>
    </div>
    <div class="service-card">
      <div class="service-icon">🎬</div>
      <h3>Video Editing</h3>
      <p>Professional video editing — reels, YouTube videos, promos, and short-form content. Smooth cuts, motion graphics, and colour grading.</p>
      <span class="service-tag">Premiere • After Effects</span>
    </div>
    <div class="service-card">
      <div class="service-icon">🎨</div>
      <h3>UI/UX Design</h3>
      <p>Intuitive designs with great visual hierarchy and user experience. Figma prototypes and wireframes delivered fast.</p>
      <span class="service-tag">Figma • Design Systems</span>
    </div>
    <div class="service-card">
      <div class="service-icon">📱</div>
      <h3>Social Media Content</h3>
      <p>Eye-catching graphics and short video clips for Instagram, YouTube, and Facebook to grow your brand online.</p>
      <span class="service-tag">Canva • Photoshop</span>
    </div>
  </div>
</section>

<!-- WHY US -->
<section id="why">
  <div class="reveal">
    <span class="section-tag">Why Choose BFS</span>
    <h2 class="section-title">Affordable. Reliable. Creative.</h2>
    <p class="section-desc">We combine quality craftsmanship with budget-friendly pricing so every business can shine online.</p>
  </div>
  <div class="why-grid reveal">
    <div class="why-item">
      <div class="why-num">01</div>
      <div>
        <h4>Low Cost Pricing</h4>
        <p>Premium quality without breaking your budget. Transparent pricing, no hidden fees.</p>
      </div>
    </div>
    <div class="why-item">
      <div class="why-num">02</div>
      <div>
        <h4>User Friendly Delivery</h4>
        <p>Easy to navigate websites and clear video outputs that your audience will love.</p>
      </div>
    </div>
    <div class="why-item">
      <div class="why-num">03</div>
      <div>
        <h4>Fast Turnaround</h4>
        <p>Deadlines are respected. Quick delivery without compromising on quality.</p>
      </div>
    </div>
    <div class="why-item">
      <div class="why-num">04</div>
      <div>
        <h4>Dedicated Support</h4>
        <p>We stay with you even after delivery — revisions, guidance, and support included.</p>
      </div>
    </div>
    <div class="why-item">
      <div class="why-num">05</div>
      <div>
        <h4>Creative Excellence</h4>
        <p>Every project is unique. We bring fresh ideas and original design to each client.</p>
      </div>
    </div>
    <div class="why-item">
      <div class="why-num">06</div>
      <div>
        <h4>100% Satisfaction</h4>
        <p>We work until you are happy. Your satisfaction is our success.</p>
      </div>
    </div>
  </div>
</section>

<!-- PRICING -->
<section id="pricing">
  <div class="reveal">
    <span class="section-tag">Pricing Plans</span>
    <h2 class="section-title">Simple & Affordable Plans</h2>
    <p class="section-desc">Choose the plan that fits your project. All plans include revisions and support.</p>
  </div>
  <div class="pricing-grid reveal">
    <div class="price-card">
      <div class="price-plan">Starter</div>
      <div class="price-amount">₹999 <span>/ project</span></div>
      <p class="price-desc">Perfect for small businesses and personal projects.</p>
      <ul class="price-features">
        <li>1-Page Website or Short Video</li>
        <li>Mobile Responsive Design</li>
        <li>2 Revision Rounds</li>
        <li>3 Day Delivery</li>
        <li>Basic Support</li>
      </ul>
      <button class="price-btn" onclick="document.getElementById('contact').scrollIntoView({behavior:'smooth'})">Get Started</button>
    </div>
    <div class="price-card popular">
      <div class="popular-badge">Most Popular</div>
      <div class="price-plan">Standard</div>
      <div class="price-amount">₹2,499 <span>/ project</span></div>
      <p class="price-desc">Best value for growing businesses and brands.</p>
      <ul class="price-features">
        <li>Up to 2-3-Page Website or Full Video</li>
        <li>Custom Design & Animations</li>
        <li>5 Revision Rounds</li>
        <li>5 Day Delivery</li>
        <li>Priority Support</li>
      </ul>
      <button class="price-btn" onclick="document.getElementById('contact').scrollIntoView({behavior:'smooth'})">Get Started</button>
    </div>
    <div class="price-card">
      <div class="price-plan">Premium</div>
      <div class="price-amount">₹4,999 <span>/ project</span></div>
      <p class="price-desc">Complete solution for professional brands and businesses.</p>
      <ul class="price-features">
        <li>Full Website + Social Media Kit</li>
        <li>Video + Motion Graphics</li>
        <li>Unlimited Revisions</li>
        <li>7 Day Delivery</li>
        <li>Dedicated Support</li>
      </ul>
      <button class="price-btn" onclick="document.getElementById('contact').scrollIntoView({behavior:'smooth'})">Get Started</button>
    </div>
  </div>
</section>

<!-- CONTACT -->
<section id="contact">
  <div class="reveal">
    <span class="section-tag">Get In Touch</span>
    <h2 class="section-title">Let's Work Together</h2>
    <p class="section-desc">Ready to start your project? Send a message and I'll get back to you within 24 hours.</p>
  </div>
  <div class="contact-wrap reveal">
    <div class="contact-info">
      <h3>Contact Information</h3>
      <p>Whether you need a website built from scratch or a video edited professionally — BFS Company is here to help at the most affordable rates.</p>
      <div class="contact-detail">
        <div class="contact-detail-icon">📧</div>
        <div><strong>Email</strong><span>lenkaprasadu74@gmail.com</span></div>
      </div>
      <div class="contact-detail">
        <div class="contact-detail-icon">📱</div>
        <div><strong>WhatsApp</strong><span>+91 9392679316</span></div>
      </div>
      <div class="contact-detail">
        <div class="contact-detail-icon">📍</div>
        <div><strong>Location</strong><span>Gangubudi, Vizianagaram, Andhra Pradesh, India</span></div>
      </div>
      <div class="contact-detail">
        <div class="contact-detail-icon">⏰</div>
        <div><strong>Working Hours</strong><span>Mon – Sat, 9 AM – 7 PM</span></div>
      </div>
    </div>
    <div>
      <div class="contact-form" id="contactForm">
        <div class="form-row">
          <div class="form-group">
            <label>Your Name</label>
            <input type="text" id="fname" placeholder="Enter your name"/>
          </div>
          <div class="form-group">
            <label>Email Address</label>
            <input type="email" id="femail" placeholder="your@email.com"/>
          </div>
        </div>
        <div class="form-group">
          <label>Service Required</label>
          <select id="fservice">
            <option value="">Select a service...</option>
            <option>Website Design</option>
            <option>Video Editing</option>
            <option>UI/UX Design</option>
            <option>Social Media Content</option>
            <option>Full Package</option>
          </select>
        </div>
        <div class="form-group">
          <label>Your Message</label>
          <textarea id="fmessage" placeholder="Tell me about your project..."></textarea>
        </div>
        <button class="form-submit" onclick="submitForm()">Send Message ✦</button>
        <div class="form-success" id="formSuccess">✅ Message sent! I'll get back to you within 24 hours.</div>
      </div>
    </div>
  </div>
</section>

<!-- FOOTER -->
<footer>
  <div class="footer-logo">BFS<span>.</span></div>
  <div class="footer-links">
    <a href="#hero">Home</a>
    <a href="#services">Services</a>
    <a href="#why">Why Us</a>
    <a href="#pricing">Pricing</a>
    <a href="#contact">Contact</a>
  </div>
  <p>© 2026 BFS Company – Best Freelancing Services. All rights reserved.<br>Website Design & Video Editing at the Lowest Cost.</p>
</footer>

<!-- SCROLL TOP -->
<button id="scrollTop" onclick="window.scrollTo({top:0,behavior:'smooth'})">↑</button>

<script>
  // ── Counter Animation ──
  function animateCounters() {
    document.querySelectorAll('[data-count]').forEach(el => {
      const target = +el.getAttribute('data-count');
      let cur = 0;
      const step = Math.ceil(target / 60);
      const timer = setInterval(() => {
        cur = Math.min(cur + step, target);
        el.textContent = cur + (target >= 100 ? '+' : target === 98 ? '%' : '+');
        if (cur >= target) clearInterval(timer);
      }, 28);
    });
  }
  const heroObserver = new IntersectionObserver(entries => {
    entries.forEach(e => { if (e.isIntersecting) { animateCounters(); heroObserver.disconnect(); } });
  }, { threshold: 0.3 });
  heroObserver.observe(document.getElementById('hero'));

  // ── Scroll Reveal ──
  const revealObserver = new IntersectionObserver(entries => {
    entries.forEach((e, i) => {
      if (e.isIntersecting) {
        setTimeout(() => e.target.classList.add('visible'), i * 100);
      }
    });
  }, { threshold: 0.12 });
  document.querySelectorAll('.reveal').forEach(el => revealObserver.observe(el));

  // ── Scroll Top Button ──
  window.addEventListener('scroll', () => {
    document.getElementById('scrollTop').classList.toggle('show', window.scrollY > 400);
  });

  // ── Mobile Menu ──
  function toggleMenu() {
    document.getElementById('mobileMenu').classList.toggle('open');
  }
  function closeMenu() {
    document.getElementById('mobileMenu').classList.remove('open');
  }

  // ── Form Submit ──
  function submitForm() {
    const name = document.getElementById('fname').value.trim();
    const email = document.getElementById('femail').value.trim();
    const service = document.getElementById('fservice').value;
    const message = document.getElementById('fmessage').value.trim();
    if (!name || !email || !service || !message) {
      alert('Please fill in all fields before submitting.');
      return;
    }
    document.getElementById('formSuccess').style.display = 'block';
    document.getElementById('fname').value = '';
    document.getElementById('femail').value = '';
    document.getElementById('fservice').value = '';
    document.getElementById('fmessage').value = '';
    setTimeout(() => document.getElementById('formSuccess').style.display = 'none', 5000);
  }

  // ── Active Nav Highlight ──
  const sections = document.querySelectorAll('section[id]');
  window.addEventListener('scroll', () => {
    let current = '';
    sections.forEach(s => {
      if (window.scrollY >= s.offsetTop - 90) current = s.getAttribute('id');
    });
    document.querySelectorAll('.nav-links a').forEach(a => {
      a.style.color = a.getAttribute('href') === '#' + current ? 'var(--blue)' : '';
    });
  });
</script>
</body>
</html>